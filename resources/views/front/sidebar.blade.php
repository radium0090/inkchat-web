<!--
<div class="col-md-4 order-md-1 mb-4 pl-1">
    <form class="card p-2 mb-3" method="GET" action="/shops">
        <div class="input-group">
            <input type="text" name="keyword" class="form-control" placeholder="キーワード">
            <div class="input-group-append">
            <button type="submit" class="btn btn-secondary btn-md my-0 ml-0">検索</button>
            </div>
        </div>
    </form>

    <ul class="list-group mb-3">
        @foreach ($categories as $cat)
            <li class="list-group-item d-flex justify-content-between lh-condensed">{{$cat->name}}</li>
        @endforeach
    </ul>

    @foreach ($tags as $tag)
        <span class="badge badge-secondary display-1">{{$tag->name}}</span>
    @endforeach

    

</div>

-->